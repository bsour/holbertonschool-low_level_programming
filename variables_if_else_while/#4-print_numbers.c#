
#include <stdio.h>

/**
 * main - print alphabets excluding q and e
 *
 * Return: 0 on success
 */

int main(void)
{
	char x = 'a';

	while (c <= 'z')
	{
		if (c != 'e' && c != 'q')
		{
			putchar(c);
		}
		c++;
	}

	putchar('\n');

	return (0);
}
