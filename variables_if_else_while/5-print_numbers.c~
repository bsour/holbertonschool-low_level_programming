#include <stdio.h>

/**
 * main - prints all base 10 single digit numbers starting from 0
 *
 * Return: 0 on success
 *
 */

int main(void)
{
	int x = 0;

	while (x <= 9)
	{
		printf("%d", x);
		x++;
	}
	printf('\n');

	return (0);
}
