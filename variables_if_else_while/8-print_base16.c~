#include <stdio.h>

/**
 * main prints all numbers of base 16
 *
 * Return: 0 on success
 *
 */

int main(void)
{
	int x = 0;

	while (x <= 15)
	{
		putchar(x + '0');
		x++;
	}
	putchar('\n');

	return (0);
}
